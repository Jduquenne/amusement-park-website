import GuestLayout from "@/components/layout/GuestLayout";

export default function GuestsGroupLayout({ children }: { children: React.ReactNode }) {
    return <GuestLayout>{children}</GuestLayout>;
}
